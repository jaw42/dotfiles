text/html;              links %s; nametemplate=%s.html
text/html;              links -dump %s; nametemplate=%s.html; copiousoutput

image/*;                ~/.scripts/fehviewer %s;

application/pdf;        evince %s;
application/pdf;        zathura %s;

application/msword;     abiword %s
